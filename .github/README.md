# GitHub configuration

此目录预留 GitHub Actions 及其他仓库协作配置。默认开发流程不创建 PR：Codex 在本地任务分支工作，由 Local 集成 Chat 合并到本地 `main`，最终由用户统一上传 GitHub。

后续 Phase 0 将按目录拆分 frontend、backend、agent、contracts 和 phase0-smoke 检查。本轮不创建占位 CI，以免发布不能执行的工作流。
