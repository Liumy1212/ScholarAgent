[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "airesearcher-agent"
version = "0.1.0.dev0"
description = "AIResearcher paper ingestion, retrieval, and tool-calling Agent API"
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "alembic>=1.16,<2",
    "fastapi>=0.115,<1",
    "httpx>=0.28,<1",
    "pydantic-settings>=2.10,<3",
    "pydantic>=2.10,<3",
    "pymupdf>=1.26,<2",
    "pymysql>=1.1,<2",
    "python-multipart>=0.0.20,<1",
    "qdrant-client>=1.15,<2",
    "sentence-transformers>=5.1,<6",
    "sqlalchemy>=2.0.40,<3",
    "uvicorn>=0.34,<1",
]

[project.optional-dependencies]
dev = [
    "jsonschema[format]>=4.23,<5",
    "mypy>=1.14,<2",
    "pytest>=8.3,<10",
    "ruff>=0.9,<1",
    "types-jsonschema>=4.23,<5",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
target-version = "py312"
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = ["ASYNC", "B", "E", "F", "I", "RUF", "UP"]
allowed-confusables = ["，", "；", "！", "？", "："]

[tool.mypy]
python_version = "3.12"
strict = true
files = ["src", "tests"]
plugins = ["pydantic.mypy"]

[tool.pytest.ini_options]
addopts = "-ra --strict-config --strict-markers"
testpaths = ["tests"]
pythonpath = [".", "src"]
